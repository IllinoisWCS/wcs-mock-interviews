const assert = require('assert');

function fn() {
  // complete this function for the interview
  // feel free to change the name of the function
  // you should also add parameters for testing

  // the function should return the computed result
  return 0;
}

function testing() {
  // format: assert(EXPECTED == ACTUAL)

  // put all test cases below this line
  // copy and paste this line and replace with correct values or function names
  assert(1 == fn());

  // put all test cases above this line

  // if the test passes, it will output "CORRECT"
  // if the test fails, an AssertionError will be thrown
  console.log("CORRECT");
}

testing();
